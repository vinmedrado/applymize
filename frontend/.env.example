# Opcional. Sem esta variável, o frontend usa o hostname atual na porta 8001.
# No Netlify, cadastre a URL HTTPS pública do backend em
# Project configuration > Environment variables.
VITE_API_BASE_URL=http://localhost:8001
